// If it was "signed by the profile's creator address"
// The creator address of the profile is likely `profile.creators?.[0]` or just `profile.addr` (which is what we fetched it by and what `signedBy` matches in roots).
// Let's check normalizeProfile
